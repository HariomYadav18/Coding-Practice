class Solution {
  public:
    int findMin(vector<int>& arr) {
        // complete the function here
        
        int n = arr.size();
        
        int res = arr[0] ;
        
        for(int i = 0; i<n ; i++){
            res=min(res,arr[i]);
            
        }
        return res;
    }
};
